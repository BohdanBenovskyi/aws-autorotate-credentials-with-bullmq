{
  "name": "aws-autorotate-credentials-with-electron",
  "version": "1.0.0",
  "description": "AWS Autorotate Credentials",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "copyMarkup": "cp -r src/renderer dist/" ,
    "start": "npm run build && npm run copyMarkup && electron ./dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "lint:check": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "author": "Bohdan Benovskyi",
  "license": "ISC",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "eslint": "^8.56.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-node": "^4.1.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "electron": "^28.2.1",
    "toastify-js": "^1.12.0"
  }
}
